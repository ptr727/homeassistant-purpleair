# artifacts
__pycache__
.pytest*
*.egg-info
*/build/*
*/dist/*


# misc
.coverage
coverage.xml
coverage.json
.ruff_cache
.claude
.codex

# Home Assistant configuration - generated locally by scripts/init-config
# (minimal, no default_config); never commit a config so the seed always runs.
config/*

# aiopurpleair editable checkout (cloned by scripts/setup)
aiopurpleair/